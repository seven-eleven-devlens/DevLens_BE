<configuration scan="true" scanPeriod="3 seconds">
    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>http://34.64.84.105:3100/loki/api/v1/push</url>
        </http>
        <format>
            <label>
                <pattern>service_name=main-application-log,host=${HOSTNAME}</pattern>
            </label>
            <message class="com.github.loki4j.logback.JsonLayout"/>
        </format>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>{"timestamp": "%d{yyyy-MM-dd HH:mm:ss}", "level": "%-5level", "message": "%msg"}%n</pattern>
        </encoder>

        <batchSize>1</batchSize>  <!-- 1개 로그마다 즉시 전송 -->
        <batchTimeoutMs>1000</batchTimeoutMs>
    </appender>

    <!-- Console에도 로그 출력 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="LOKI"/>
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
